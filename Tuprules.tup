# Project wide definitions and rule macros go here.

# Base paths.
_               = $(TUP_CWD)
TEMP_DIR        = /tmp

include build_defs/unixen.tup

include build_defs/ar.tup
include build_defs/c.tup
include build_defs/cplusplus.tup
include build_defs/go.tup
